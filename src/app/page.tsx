import Home from "@/components/Home";
import React from "react";
// import { gsap } from "gsap";
// import { CSSPlugin } from "gsap/CSSPlugin";
// gsap.registerPlugin(CSSPlugin);

const page = () => {
  return (
    <div style={{ width: "100%", margin: "2vh 2vh" }}>
      <Home />
    </div>
  );
};

export default page;
